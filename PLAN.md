# SCOTTCHEN 下一阶段 SEO 一次性执行计划

更新时间：2026-09-15

## 最终目标

在一个连续执行批次内完成剩余 SEO 优化、验证、提交、推送、Vercel
生产部署和线上复验。执行期间无需用户反复回复“继续”。

本批次完成后应达到：

- 搜索引擎可稳定抓取和理解首页、产品、服务、方案与资源页面。
- 英文和中文核心页面的搜索意图清晰，内部链接不互相争抢关键词。
- 普通询价、样品和替代方案入口都保留来源页面，便于判断真实转化贡献。
- 企业实体、产品、面包屑、网站和 AI 抓取信息保持一致。
- 桌面端与移动端不存在关键横向滚动、资源错误或明显性能回退。
- 所有本次改动通过检查后一次提交、一次生产发布并完成匿名线上验收。

## 执行边界

- 保护巡检开始前已经存在的用户改动，不覆盖、不清理、不混入提交。
- 只精确暂存本批次修改且已验证的文件。
- 不虚构认证、客户、案例、产能、规格、地址、电话或社交账号。
- 2026-09-08 上线的搜索意图实验页面保持稳定；没有新 GSC 数据证据时不改其核心标题和定位。
- 不为了页面数量创建重复、薄弱或无真实证据支撑的内容。
- 在已有持续技术授权范围内连续执行；只有付款、账号创建或删除、密钥处置、域名转移、不可逆删除，或无法隔离用户改动时才暂停。

## 阶段 A：恢复数据基线

预计 20–35 分钟。

执行内容：

- 读取 automation memory、当前分支、远端、最近提交和未提交改动。
- 恢复或确认 Google Search Console 只读访问。
- 使用 `sc-domain:scottchentools.com` 获取最近 28 天、前 28 天、最近 7 天和前 7 天数据。
- 按查询、页面、国家、设备分析点击、展示、点击率和平均排名。
- 检查 sitemap、索引覆盖和重要 URL 的可检查状态。
- 保存本次基线，避免后续重复请求。

完成标准：

- 数据窗口和口径可复核，或明确记录当前机器缺少访问范围及已有最近基线。
- 有数据证据时才调整 9 月 8 日实验页；没有证据时保持稳定。

## 阶段 B：补齐询价来源追踪

预计 25–40 分钟。

执行内容：

- 扫描英文和中文公开页面的普通 `/contact` 与 `/sample-request` 链接。
- 把核心询价、样品、替代和定制入口统一接入已有来源参数机制。
- 保证链接仍存在于服务端输出的原始 HTML 中，不依赖 JavaScript 才出现。
- 保留页面原有文案和业务事实，仅修正转化归因。

完成标准：

- 核心商业页面的 CTA 可追溯到来源页面和意图。
- 联系与样品页面能读取来源参数。
- 无失效链接、重复嵌套参数或客户端专属入口。

## 阶段 C：搜索意图与内链优化

预计 35–55 分钟。

执行内容：

- 建立首页、产品、行业应用、OEM、替代采购、服务能力和资源页面的意图地图。
- 检查标题、H1、正文和内部锚文本是否产生关键词重叠。
- 调整有证据支持的内部链接，让产品页连接服务与采购场景，让资源页连接相应产品与询价入口。
- 检查 `/services`、`/how-it-works` 及旧路径的 301/404 行为。
- 修复站内 404、循环跳转、孤立页面和不一致的中英文对应关系。

完成标准：

- 每个核心搜索意图有一个明确主页面。
- 核心页面可在合理点击深度内到达。
- 不存在会误导用户或搜索引擎的重定向和软 404。

## 阶段 D：元数据、结构化数据与 AI 发现

预计 25–40 分钟。

执行内容：

- 核对主要页面唯一的 title、description、H1、canonical 和 hreflang。
- 验证 Organization、WebSite、Product、BreadcrumbList 数据。
- 只使用仓库和官网中可核验的企业与产品事实。
- 核对 `robots.txt`、`sitemap.xml`、`llms.txt` 与真实公开路由一致。
- 检查 Markdown 内容协商；仅在网站确实返回 Markdown 时设置正确的 `Vary`。
- 统一企业名称、域名、logo、联系方式和页面实体关系。

完成标准：

- JSON-LD 可解析且引用真实 URL。
- canonical、hreflang、sitemap 和 robots 没有互相冲突。
- AI 抓取说明覆盖产品、行业、OEM、替代采购、服务、案例/资源及联系路径。

## 阶段 E：技术、移动端与性能检查

预计 30–50 分钟。

执行内容：

- 先运行现有 `seo:smoke`，再按改动范围运行 lint、typecheck、build 和浏览器 smoke。
- 检查首页、核心产品页和重点采购页的原始 HTML。
- 检查状态码、重定向、安全头、canonical、hreflang、图片尺寸与替代文本。
- 在桌面和移动视口检查布局、横向滚动、CTA 可见性和表单可用性。
- 对关键页面做 PSI/Lighthouse 复测，优先处理可验证的 Core Web Vitals 回退。

完成标准：

- 构建和适用检查通过。
- 核心页面原始 HTML 含公司定位、主要产品、采购场景、服务能力及询价入口。
- 移动端无关键横向滚动或遮挡，资源与响应状态正常。

## 阶段 F：询盘与邮件链路闭环

预计 20–35 分钟。

执行内容：

- 验证 Contact、RFQ 和 sample 表单的字段、校验、来源参数和成功/失败状态。
- 验证邮件 fallback 可见且可使用。
- 优先采用无投递验证；只有本次修改生产链路且无法无投递验证时，发送一次明确标注的自动测试询盘。
- 确认页面来源能够进入现有分析或后台接收链路。

完成标准：

- 三类表单路径可访问、可提交或有可用 fallback。
- 来源信息不会在跳转或提交过程中丢失。
- 不产生重复测试询盘。

## 阶段 G：统一提交、部署与生产验收

预计 25–45 分钟；Vercel 外部等待通常 5–15 分钟。

执行内容：

- 复查差异，只精确暂存本批次文件。
- 创建一条清晰 commit，正常推送当前 `main`，禁止 force push。
- 等待现有 Vercel Production Deployment 完成。
- 对 `https://www.scottchentools.com` 做匿名生产 smoke。
- 复验首页、产品页、服务/旧路径、错误 URL、robots、sitemap、llms、结构化数据、表单和关键静态资源。
- sitemap 只有新增/改变、GSC 报错、长期未读取或提交缺失时才重新提交。
- IndexNow 只用于本批次新增或变化的公开 URL，不声称会通知 Google。
- 将新增证据、执行结果和去重状态追加到 automation memory。

完成标准：

- commit hash、push 结果、部署标识或生产 URL均有记录。
- 生产状态码、页面内容、CTA、结构化数据和核心资源复验通过。
- 用户已有未提交改动保持原样并在报告中列明。

## 一次性交付内容

最终报告固定包含：

- 已采集数据
- 本地修复
- 验证
- 需要外部授权
- 未执行动作
- commit hash、push 结果、部署信息和生产验证结果
- 实际存在且未被混入提交的用户改动
- 原预计与实际耗时对照

## 总耗时与检查点

- Codex 连续执行预计：2–4 小时。
- 外部部署等待：约 5–15 分钟，实际取决于 Vercel。
- 首个可检查成果：开始后约 35 分钟，完成数据基线与改动范围冻结。
- 中间检查点：约 90 分钟，完成来源追踪与搜索意图/内链修改。
- 最终检查点：本地验证、统一提交、生产部署和线上复验全部完成。

Google 排名、点击和询盘变化需要自然积累。生产发布后继续观察 28 天，
分别比较 7 天和 28 天等长窗口；这段等待不影响本次代码与部署交付完成。

## 后续效果判断

上线后只追踪有业务意义的变化：

- GSC 非品牌展示、点击、点击率与目标查询平均排名。
- 核心产品和采购场景页面的曝光、落地页分布及关键词互相争抢情况。
- RFQ、样品和联系入口按来源页面的到达与提交情况。
- 已抓取/已索引状态、Core Web Vitals 和关键生产错误。

健康且未变化的项目只记录“与上次一致”，不重复列出整套历史清单。
